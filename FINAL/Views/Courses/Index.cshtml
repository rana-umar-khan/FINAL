@model IEnumerable<FINAL.Course>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CouName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.TeaUsername)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CouName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.TeaUsername)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CouId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CouId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CouId })
        </td>
    </tr>
}

</table>
